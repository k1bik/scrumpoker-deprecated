<% if room.show_estimates %>
  <%= button_to "Hide results", toogle_estimates_path, params: {room_id: room.id, show: false}, class: "button-border" %>
<% else %>
  <%= button_to "Show results", toogle_estimates_path, params: {room_id: room.id, show: true}, class: "button-border" %>
<% end %>
